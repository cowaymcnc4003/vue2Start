export default {
    fetchedUser(state) {
        return state.user;
    },
    fetchedJobs(state) {
        return state.jobs;
    },
    fetchedAsk(state) {
        return state.ask;
    },
    fetchedList(state) {
        return state.newsList;
    }
}